// src/plugins/vuetify.js

// Vuetify 3 imports
import 'vuetify/styles';  // Ensure Vuetify's styles are imported
import { createVuetify } from 'vuetify';

// Create the Vuetify instance
export default createVuetify({
  // You can add theme customization or other Vuetify settings here if needed
});
